import React from 'react';

const navigation = (props)=>{
    return(
    <div className="top-heading">
    <span className="back-arrow"  id="webview-back"></span>
    <span className="mr-345">{'Search Results'}</span>
    </div>)
}

export default navigation; 